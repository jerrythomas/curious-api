language: node_js
node_js:
- 6 
addons:
  apt:
    packages:
       libaio1
       build-essential
       curl
       unzip
before_install:
  - source ./scripts/node_oracle_env.sh
env:
  - PORT=8000
  - BASE_URL=http://localhost:8000
  - PACKAGE_VERSION="1.0.*"
notifications:
  email: false
